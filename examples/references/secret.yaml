# A Kubernetes Secret holding the client secret. The KeycloakClient in this directory pulls its
# `secret` from here through valuesFrom, so the committed CR never carries the value in clear.
apiVersion: v1
kind: Secret
metadata:
  name: reference-example-secret
type: Opaque
stringData:
  client-secret: super-secret-value
