# Read-only tests boot against the CRs written by the write-mode boot (the per-run test
# namespace in the kind cluster survives embedded-server restarts), so class order matters.
junit.jupiter.testclass.order.default=org.junit.jupiter.api.ClassOrderer$OrderAnnotation
